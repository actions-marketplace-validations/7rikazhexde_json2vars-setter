# Bun

<!-- AUTO-GENERATED by .github/scripts/gen_language_docs.py - DO NOT EDIT.
     Edit the source matrix JSON / workflow and re-run the generator instead. -->

How `json2vars-setter` handles Bun, and how the example workflow provisions it.

## json2vars-setter integration

| Item | Value |
| --- | --- |
| Matrix JSON key | `versions.bun` |
| Action output | `versions_bun` |
| Dynamic-update strategy input | `bun-strategy` (`stable` / `latest` / `both`) |
| Version source | see [Version Sources](../reference/version-sources.md) |

## Supported runners

The example matrix runs on **Ubuntu / Windows / macOS**:

`ubuntu-latest`, `windows-latest`, `macos-latest`

Any GitHub-hosted runner the setup action supports also works - the OS list is just the `os` array in your matrix JSON.

## Supported versions

Example matrix (what the example workflow runs):

```json
{
  "versions": {
    "bun": [
      "1.2.x",
      "1.3.x"
    ]
  }
}
```

Latest values tracked by the dynamic-update tooling (auto-refreshed from the release APIs into the version cache):

| Field | Version |
| --- | --- |
| `stable` | `1.2.23` |
| `latest` | `1.3.14` |

_Cache last updated: 2026-06-24T12:28:41.753369._

## Setup action (example workflow)

[`bun_test.yml`](https://github.com/7rikazhexde/json2vars-setter/blob/main/.github/workflows/bun_test.yml) provisions Bun with the matrix value json2vars-setter produced:

```yaml
- name: Set up Bun
  uses: oven-sh/setup-bun@v2.2.0
  with:
    bun-version: ${{ matrix.bun-version }}
```

## References

- Example project: [`examples/bun/`](https://github.com/7rikazhexde/json2vars-setter/tree/main/examples/bun)
- Example workflow: [`.github/workflows/bun_test.yml`](https://github.com/7rikazhexde/json2vars-setter/blob/main/.github/workflows/bun_test.yml)
- Matrix JSON: [`examples/bun/bun_project_matrix.json`](https://github.com/7rikazhexde/json2vars-setter/blob/main/examples/bun/bun_project_matrix.json)
