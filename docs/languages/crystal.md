# Crystal

<!-- AUTO-GENERATED by .github/scripts/gen_language_docs.py - DO NOT EDIT.
     Edit the source matrix JSON / workflow and re-run the generator instead. -->

How `json2vars-setter` handles Crystal, and how the example workflow provisions it.

## json2vars-setter integration

| Item | Value |
| --- | --- |
| Matrix JSON key | `versions.crystal` |
| Action output | `versions_crystal` |
| Dynamic-update strategy input | `crystal-strategy` (`stable` / `latest` / `both`) |
| Version source | see [Version Sources](../reference/version-sources.md) |

## Supported runners

The example matrix runs on **Ubuntu / macOS**:

`ubuntu-latest`, `macos-latest`

Any GitHub-hosted runner the setup action supports also works - the OS list is just the `os` array in your matrix JSON.

## Supported versions

Example matrix (what the example workflow runs):

```json
{
  "versions": {
    "crystal": [
      "1.19.2",
      "1.20.2"
    ]
  }
}
```

Latest values tracked by the dynamic-update tooling (auto-refreshed from the release APIs into the version cache):

| Field | Version |
| --- | --- |
| `stable` | `1.19.2` |
| `latest` | `1.20.2` |

_Cache last updated: 2026-06-24T12:28:43.551556._

## Setup action (example workflow)

[`crystal_test.yml`](https://github.com/7rikazhexde/json2vars-setter/blob/main/.github/workflows/crystal_test.yml) provisions Crystal with the matrix value json2vars-setter produced:

```yaml
- name: Set up Crystal
  uses: crystal-lang/install-crystal@v1.9.2
  with:
    crystal: ${{ matrix.crystal-version }}
```

## References

- Example project: [`examples/crystal/`](https://github.com/7rikazhexde/json2vars-setter/tree/main/examples/crystal)
- Example workflow: [`.github/workflows/crystal_test.yml`](https://github.com/7rikazhexde/json2vars-setter/blob/main/.github/workflows/crystal_test.yml)
- Matrix JSON: [`examples/crystal/crystal_project_matrix.json`](https://github.com/7rikazhexde/json2vars-setter/blob/main/examples/crystal/crystal_project_matrix.json)
