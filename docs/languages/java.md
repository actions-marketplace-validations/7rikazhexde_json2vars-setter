# Java

<!-- AUTO-GENERATED by .github/scripts/gen_language_docs.py - DO NOT EDIT.
     Edit the source matrix JSON / workflow and re-run the generator instead. -->

How `json2vars-setter` handles Java, and how the example workflow provisions it.

## json2vars-setter integration

| Item | Value |
| --- | --- |
| Matrix JSON key | `versions.java` |
| Action output | `versions_java` |
| Dynamic-update strategy input | `java-strategy` (`stable` / `latest` / `both`) |
| Version source | see [Version Sources](../reference/version-sources.md) |

## Supported runners

The example matrix runs on **Ubuntu / Windows / macOS**:

`ubuntu-latest`, `windows-latest`, `macos-latest`

Any GitHub-hosted runner the setup action supports also works - the OS list is just the `os` array in your matrix JSON.

## Supported versions

Example matrix (what the example workflow runs):

```json
{
  "versions": {
    "java": [
      "11",
      "17",
      "21"
    ]
  }
}
```

Latest values tracked by the dynamic-update tooling (auto-refreshed from the release APIs into the version cache):

| Field | Version |
| --- | --- |
| `stable` | `25` |
| `latest` | `26` |

_Cache last updated: 2026-06-24T12:28:41.325097._

## Setup action (example workflow)

[`java_test.yml`](https://github.com/7rikazhexde/json2vars-setter/blob/main/.github/workflows/java_test.yml) provisions Java with the matrix value json2vars-setter produced:

```yaml
- name: Set up Java
  uses: actions/setup-java@v5.7.0
  with:
    distribution: 'temurin'
    java-version: ${{ matrix.java-version }}
    cache: 'maven'
```

## References

- Example project: [`examples/java/`](https://github.com/7rikazhexde/json2vars-setter/tree/main/examples/java)
- Example workflow: [`.github/workflows/java_test.yml`](https://github.com/7rikazhexde/json2vars-setter/blob/main/.github/workflows/java_test.yml)
- Matrix JSON: [`examples/java/java_project_matrix.json`](https://github.com/7rikazhexde/json2vars-setter/blob/main/examples/java/java_project_matrix.json)
