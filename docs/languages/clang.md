# Clang / LLVM

<!-- AUTO-GENERATED by .github/scripts/gen_language_docs.py - DO NOT EDIT.
     Edit the source matrix JSON / workflow and re-run the generator instead. -->

How `json2vars-setter` handles Clang / LLVM, and how the example workflow provisions it.

## json2vars-setter integration

| Item | Value |
| --- | --- |
| Matrix JSON key | `versions.clang` |
| Action output | `versions_clang` |
| Dynamic-update strategy input | `clang-strategy` (`stable` / `latest` / `both`) |
| Version source | see [Version Sources](../reference/version-sources.md) |

## Supported runners

The example matrix runs on **Ubuntu / macOS**:

`ubuntu-latest`, `macos-latest`

Any GitHub-hosted runner the setup action supports also works - the OS list is just the `os` array in your matrix JSON.

## Supported versions

Example matrix (what the example workflow runs):

```json
{
  "versions": {
    "clang": [
      "20.1.8",
      "19.1.7"
    ]
  }
}
```

Latest values tracked by the dynamic-update tooling (auto-refreshed from the release APIs into the version cache):

| Field | Version |
| --- | --- |
| `stable` | `21.1.8` |
| `latest` | `22.1.8` |

_Cache last updated: 2026-06-24T12:28:47.118004._

## Setup action (example workflow)

[`clang_test.yml`](https://github.com/7rikazhexde/json2vars-setter/blob/main/.github/workflows/clang_test.yml) provisions Clang / LLVM with the matrix value json2vars-setter produced:

```yaml
- name: Set up Clang ${{ matrix.clang-version }}
  uses: aminya/setup-cpp@v1.8.1
  with:
    compiler: llvm-${{ matrix.clang-version }}
```

> **Note:** `aminya/setup-cpp` is the de-facto cross-platform compiler installer (no official Clang setup action exists); it exports `CC`/`CXX` pointing at the installed LLVM clang. The C++ *standard* (`-std=c++20`, ...) is a static list you hand-write in the matrix JSON - it needs no fetcher.

## References

- Example project: [`examples/clang/`](https://github.com/7rikazhexde/json2vars-setter/tree/main/examples/clang)
- Example workflow: [`.github/workflows/clang_test.yml`](https://github.com/7rikazhexde/json2vars-setter/blob/main/.github/workflows/clang_test.yml)
- Matrix JSON: [`examples/clang/clang_project_matrix.json`](https://github.com/7rikazhexde/json2vars-setter/blob/main/examples/clang/clang_project_matrix.json)
