# Deno

<!-- AUTO-GENERATED by .github/scripts/gen_language_docs.py - DO NOT EDIT.
     Edit the source matrix JSON / workflow and re-run the generator instead. -->

How `json2vars-setter` handles Deno, and how the example workflow provisions it.

## json2vars-setter integration

| Item | Value |
| --- | --- |
| Matrix JSON key | `versions.deno` |
| Action output | `versions_deno` |
| Dynamic-update strategy input | `deno-strategy` (`stable` / `latest` / `both`) |
| Version source | see [Version Sources](../reference/version-sources.md) |

## Supported runners

The example matrix runs on **Ubuntu / Windows / macOS**:

`ubuntu-latest`, `windows-latest`, `macos-latest`

Any GitHub-hosted runner the setup action supports also works - the OS list is just the `os` array in your matrix JSON.

## Supported versions

Example matrix (what the example workflow runs):

```json
{
  "versions": {
    "deno": [
      "v1.x",
      "v2.x"
    ]
  }
}
```

Latest values tracked by the dynamic-update tooling (auto-refreshed from the release APIs into the version cache):

| Field | Version |
| --- | --- |
| `stable` | `2.7.14` |
| `latest` | `2.8.3` |

_Cache last updated: 2026-06-24T12:28:41.530983._

## Setup action (example workflow)

[`deno_test.yml`](https://github.com/7rikazhexde/json2vars-setter/blob/main/.github/workflows/deno_test.yml) provisions Deno with the matrix value json2vars-setter produced:

```yaml
- name: Set up Deno
  uses: denoland/setup-deno@v2.0.5
  with:
    deno-version: ${{ matrix.deno-version }}
```

## References

- Example project: [`examples/deno/`](https://github.com/7rikazhexde/json2vars-setter/tree/main/examples/deno)
- Example workflow: [`.github/workflows/deno_test.yml`](https://github.com/7rikazhexde/json2vars-setter/blob/main/.github/workflows/deno_test.yml)
- Matrix JSON: [`examples/deno/deno_project_matrix.json`](https://github.com/7rikazhexde/json2vars-setter/blob/main/examples/deno/deno_project_matrix.json)
