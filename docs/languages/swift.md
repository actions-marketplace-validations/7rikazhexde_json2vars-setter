# Swift

<!-- AUTO-GENERATED by .github/scripts/gen_language_docs.py - DO NOT EDIT.
     Edit the source matrix JSON / workflow and re-run the generator instead. -->

How `json2vars-setter` handles Swift, and how the example workflow provisions it.

## json2vars-setter integration

| Item | Value |
| --- | --- |
| Matrix JSON key | `versions.swift` |
| Action output | `versions_swift` |
| Dynamic-update strategy input | `swift-strategy` (`stable` / `latest` / `both`) |
| Version source | see [Version Sources](../reference/version-sources.md) |

## Supported runners

The example matrix runs on **Ubuntu / macOS**:

`ubuntu-latest`, `macos-latest`

Any GitHub-hosted runner the setup action supports also works - the OS list is just the `os` array in your matrix JSON.

## Supported versions

Example matrix (what the example workflow runs):

```json
{
  "versions": {
    "swift": [
      "6.2",
      "6.2.1"
    ]
  }
}
```

Latest values tracked by the dynamic-update tooling (auto-refreshed from the release APIs into the version cache):

| Field | Version |
| --- | --- |
| `stable` | `6.2.4` |
| `latest` | `6.3.2` |

_Cache last updated: 2026-06-24T12:28:42.733299._

## Setup action (example workflow)

[`swift_test.yml`](https://github.com/7rikazhexde/json2vars-setter/blob/main/.github/workflows/swift_test.yml) provisions Swift with the matrix value json2vars-setter produced:

```yaml
- name: Set up Swift
  uses: swift-actions/setup-swift@v2.4.0
  with:
    swift-version: ${{ matrix.swift-version }}
```

> **Note:** `swift-actions/setup-swift` installs from its own bundled list of known Swift versions, which can lag the newest swift.org release - pin to a version the action supports if the very latest is not yet available.

## References

- Example project: [`examples/swift/`](https://github.com/7rikazhexde/json2vars-setter/tree/main/examples/swift)
- Example workflow: [`.github/workflows/swift_test.yml`](https://github.com/7rikazhexde/json2vars-setter/blob/main/.github/workflows/swift_test.yml)
- Matrix JSON: [`examples/swift/swift_project_matrix.json`](https://github.com/7rikazhexde/json2vars-setter/blob/main/examples/swift/swift_project_matrix.json)
