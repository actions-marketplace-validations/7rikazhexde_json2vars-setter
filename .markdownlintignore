# Generated by semantic-release (semantic-release-gitmoji); its heading
# format triggers MD022. The file is regenerated on every release, so lint
# fixes would not persist — exclude it from markdownlint.
CHANGELOG.md
